/**
 * Translater program.
 * 
 * @author Patrick Muradaz
 * @version 3-30-2018
 * 
 * Acknowledgments: I have received no unauthorized help on this
 * assignment.
 */
public class DukesTranslate {

    /**
     * Counts the number of characters, words, and newlines.
     * Returns an array of these counts.
     * 
     * @param str is a string to be analyzed.
     * @return an array containing counts for characters, words, and newlines.
     */
    public static int[] analyze(String str) {
        int[] count = new int[] {0, 0, 0};
        
        
        return count;
    }

    /**
     * Returns a new string modified such that any word with an 'm' or 'M'
     * character now starts with a 'j' and ends with a 'u'.
     * 
     * @param str is an input string.
     * @return a new string.
     */
    public static String jmuIt(String str) {
        String newStr = str;
        
        for (int i = 0; i < str.length; i++) {
            
        }
        return newStr;
    }

    /**
     * Returns an array of sentences found in a given string.
     * 
     * @param str is an input string.
     * @return an array of sentences.
     */
    public static String[] separate(String str) {
        String[] string = new String[] {"0", "0", "0"};
        
        
        return string;
    }

    /**
     * Returns a string with given acronyms replaced with given definitions.
     * 
     * @param acronym is an array of acronyms.
     * @param definition is an array of acronym definitions.
     * @param text is an array of text containing acronyms.
     * @return a string of text with definitions substituted for acronyms.
     */
    public static String translate(String[] acronym, String[] definition,
            String text) {
        
        
        return "null";
    }
}
